       <mp-cells>
            <mp-cell hover link wx:for="{{newsList}}"  wx:for-index="index" wx:for-item="item" url="{{'../infoDetail/index?obj=' + item.summary}}" value="{{item.title}}" singleId="{{item.id}}" wx:key="index"></mp-cell>
        </mp-cells>
