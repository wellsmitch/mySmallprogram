       <mp-cells>
            <mp-cell hover link wx:for="{{newsList}}"  wx:for-index="index" wx:for-item="item" bindtap="tap" data-value="{{item}}" value="{{item.title}}" singleId="{{item.id}}" wx:key="index"></mp-cell>
        </mp-cells>
